export interface Booking {
    bookingId?: number,
    bookingUser?: string,
    phoneNumber?: string,
    createTs?: string,
    updateTs?: string,
    lessonName?: string,
    lessonPrice?: number,
}